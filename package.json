{
  "name": "gershon-pulse",
  "version": "3.0.0",
  "private": true,
  "description": "Pulse — LinkedIn Message Triage Dashboard (Cloudflare Pages)",
  "scripts": {
    "dev": "npx wrangler pages dev app",
    "deploy": "npx wrangler pages deploy app --project-name gershon-pulse"
  }
}
